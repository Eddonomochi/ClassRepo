@model Dapper101AHBCJULY2019.Models.CustomersViewModel

@{
    ViewData["Title"] = "Customers";
}

<h1>CustomerViewModels</h1>

<div>
    <ul>
        @foreach (var customer in Model.Customers)
        {
            <li>
                <a asp-controller="Northwind" asp-action="Customer" asp-route-id="@customer.Id">@customer.Name</a>
            </li>
        }
    </ul>
</div>


<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
